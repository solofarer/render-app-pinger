name: Ping Render App

on:
  schedule:
    - cron: '*/14 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render app URL
        run: curl -fsSL "$RENDER_APP_URL" || exit 1
        env:
          RENDER_APP_URL: ${{ vars.RENDER_APP_URL }}
